#          Copyright Rein Halbersma 2014-2025.
# Distributed under the Boost Software License, Version 1.0.
#    (See accompanying file LICENSE_1_0.txt or copy at
#          http://www.boost.org/LICENSE_1_0.txt)

name: Consumption

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # The last stub to arrive: the shared workflow could not express this library before cpp-ci v0.4.1.
  consumption:
    uses: rhalbersma/cpp-ci/.github/workflows/consumption.yml@e2886b13f6fcd1eceb231652f816b016de41be1d # v0.8.1
    with:
      # CMakeLists.txt find_package()s boost_hash2 unconditionally, so every configure here needs vcpkg.
      vcpkg: true
      # The revision CMakeLists.txt pins; install(EXPORT) cannot export a FetchContent build tree, so an installed xstd is required.
      dependency_repos: |
        https://github.com/rhalbersma/xstd.git 42ab0d409fc9118ea0dc8846508ea62e97b90c7e
