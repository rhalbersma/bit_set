#          Copyright Rein Halbersma 2014-2025.
# Distributed under the Boost Software License, Version 1.0.
#    (See accompanying file LICENSE_1_0.txt or copy at
#          http://www.boost.org/LICENSE_1_0.txt)

name: CodeQL

permissions:
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '17 3 * * 1'
  workflow_dispatch:

jobs:
  # No inputs: c-cpp with security-extended is what the local job ran and
  # what the shared workflow defaults to.
  codeql:
    uses: rhalbersma/cpp-ci/.github/workflows/codeql.yml@761f20d7d737d4e0c5a942eb2f1becf8e8edea75 # v0.3.1
